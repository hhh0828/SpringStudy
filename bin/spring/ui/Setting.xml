<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
	
	<bean id="hexam" class="spring.ui.di.Hexam" />
	<!--객체의 생성 부분 Hexam class를 참조하여 hexam 으로 만든다.-->
	<bean id="jexam" class="spring.ui.di.Jexam" />
	<!--객체의 생성 부분 Jexam class를 참조하여 jexam 으로 만든다.-->
	
	<bean id="total" class="spring.ui.di.mi.Total">
		<property name="exam" ref="jexam"></property>
	</bean>
	<bean id="average" class="spring.ui.di.mi.Average">
		<property name="exam" ref="hexam"></property>
	
	</bean>
	
	
	<!-- 객체 생성과 결합! 
	id 에들어가는것은 그객체를 어떤이름으로 쓰려고하는지
	class 에들어가는것은 어떤클래스를 객체화할것인지 -->
	
	<!-- property 태그를 사용한 객체 주입 
	name 객체를 쓸때 자바코드에 override한 setExam 함수를 부른다 앞에set생략
	ref 에들어가는것은 객체 타입이 레퍼런스 참조형이면 ref사용하고 위에서 생성한 객체를 꽂아줌 
	value 에들어가는것은 객체 타입이 기본형 이면 사용. 프리미티브 타입
	-->
	
	
	

</beans>
